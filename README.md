# Next.js Project


### Run the development server:

```bash
npm install
npm run dev
```

### Build for production AND generate static website into "out" folder:

```bash
npm run build
```

### Run in production:

```bash
npm start
```
